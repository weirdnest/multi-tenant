export enum RolesMessage {
  ALREADY_EXISTS = `Role with such name already exists`,
  EMPTY_ROLE_NAME = `Role name should not be empty`,
  MISSING_ROLE_ID = `Missing role id`,
  NOT_FOUND = `Role not found`,
}
